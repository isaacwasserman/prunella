#!/usr/bin/env sh
set -e

# Auto-fix lint/format issues before committing, then re-stage the fixes.
# If biome reports errors it can't fix, this exits non-zero and blocks the commit.
bun run lint --write --unsafe
git add -u src
